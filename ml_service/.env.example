# ===========================================================================
# ML Service（Python）環境變數範本 — 複製為 .env 並填入實際值
#   cp .env.example .env
# .env 已被 .gitignore 忽略；切勿 commit 真實金鑰。
# ===========================================================================

# 與後端 ML_API_KEY 對應，用於驗證後端代理請求
ML_API_KEY=your_ml_api_key_here

# 服務埠
PORT=8100

# --- 模型 / 推論設定 ---
ML_DEPTH_MODEL=da_v2_base
ML_ENABLE_SAM=true
ML_SEG_MODEL=sam2_tiny
# CORS 允許來源（多個用逗號分隔）
ML_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
ML_DEBUG=false
ML_RATE_LIMIT=30

# --- 效能（依硬體調整）---
ML_CPU_THREADS=6
OMP_NUM_THREADS=4
MKL_NUM_THREADS=4

# --- 訓練腳本用（trunk_detector_training/，非 runtime）---
# 取得：Kaggle 帳號 Settings → API；Roboflow 帳號 Settings → API key
KAGGLE_USERNAME=your_kaggle_username
KAGGLE_KEY=your_kaggle_key
ROBOFLOW_API_KEY=your_roboflow_key
